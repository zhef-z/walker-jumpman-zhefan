# Raw engine captures and rendered masters are large and reproducible.
# The drivers, paperwork and coverage contract are committed; the bytes are not.
capture/
exports/
media/

# Generated build output from the compiler. The film is reproducible from the
# beat sheet, the capture driver and the paperwork; these are caches and carry
# absolute local paths.
clips/
build-state.json
